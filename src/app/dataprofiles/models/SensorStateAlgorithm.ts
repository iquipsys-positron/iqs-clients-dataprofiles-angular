export enum SensorStateAlgorithm {
    Custom = 'custom', // Whatever user decides
    Duration = 'duration', // Calculate total duration
    Utilization = 'utilization' // Calculate total utilization
}
